//type for custom search props

import type { PrimitiveTypes } from "../../../types/basicTypes";

type CustomSearchProps = {
  options: PrimitiveTypes["string"][];
};

export type { CustomSearchProps };
